import user from './user'
import api from './api'
import product from './product'
import token from './token'

const routes = {
  user: user,
  api: api,
  product: product,
  token: token
}

export default routes
