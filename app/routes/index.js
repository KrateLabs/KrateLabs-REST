import user from './user'
import api from './api'
import product from './product'
import log from './log'

const routes = {
  user: user,
  api: api,
  product: product,
  log: log
}

export default routes
